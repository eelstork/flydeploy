[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "flydeploy"
version = "0.1.0"
description = "Shared Fly.io deployment toolkit"
requires-python = ">=3.10"
